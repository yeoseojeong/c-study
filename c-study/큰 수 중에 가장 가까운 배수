#include<stdio.h>
int main()
{
    int n, m;
    int cut = 0;
    scanf("%d %d", &n, &m);
     
 
    for (int i = n+1; i>=m ; i++)
    {
        if (i % m == 0)
        {
            cut = i;
            break;
        }
    }
    for (int i = 1; i <= cut; i++)
    {
        printf("%d ", i);
    }
    return 0;
}
