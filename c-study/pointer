
//strlen 사용하지 않고 포인터 변수와 for문을 사용하여 문자 배열의 길이 구하기
#include<stdio.h>

int str_len(char* p)
{
	int len=0;

	while (*p++)
	{
		len++;
	}
	return len;
}

int main(void)
{
	char str[100] = {};

	int cnt = 0;

	gets_s(str);





	printf("%d", str_len(str));
}
