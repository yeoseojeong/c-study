#include<stdio.h>
int main()
{
    unsigned int n;
    unsigned int result[101];
    unsigned int result_2[101];
    int len = 0;
    int cnt = 0;
    int index;
 
    scanf("%d %d", &n,&index);
 
 
    while (1)
    {
        result[cnt] = n % 2;
        n = n / 2;
        cnt++;
         
        if (n == 0)
        {
            break;
        }
    }
         
            printf("%d", result[index]);
            return 0;
}
