//포인터 1581 

#include <stdio.h>
void myswap(int* x, int* y)
{
    int temp=0;
    if (*x > *y)
    {
        temp = *y;
        *y = *x;
        *x = temp;
    }
} 
// 이 부분에 들어가야 될 코드를 작성하여 제출
main()
{
    int a, b;
    scanf("%d%d", &a, &b);
    myswap(&a, &b);
    printf("%d %d", a, b);
}


